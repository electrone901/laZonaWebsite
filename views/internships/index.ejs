<% include ../partials/header %>

    <header class="jumbotron" id="orange">
        <div class="container">
            <h1>Internado</h1>
            <p>
                <a class="btn btn-primary btn-lg" href="/internships/new">Añadir una Nueva Pasantía</a>
            </p>
            <form action="/internships/title" method="GET" class="form-inline">
                <div class="form-group">
                    <input type="text" name="search" placeholder="Búsqueda por título..." class="form-control">
                    <button class="btn btn-default">
                        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                        Buscar
                    </button>
                </div>
            </form>
        </div>
    </header>
    
    <div class="row">
        <div class="col-md-12 text-center">
            <% if(noMatch !== null){ %>
                <h3><%= noMatch %></h3></br>
            <% } %>
        </div>
        <% internships.forEach(function(internship){ %>
            <div class="col-md-12 col-sm-6">
                <div class="thumbnail ridgeBorder">
                    <a href="/internships/<%= internship._id %>"><p><%= internship.title %></p></a>
                    <p><%- internship.description.substring(0, 100) %>...</p>
                    <span><%= moment(internship.createdAt).fromNow() %> by <a href="/profile/<%= internship.author.id %>"><%= internship.author.username %></span></a>
                </div>
            </div>
        <% }); %>
    </div>

<% include ../partials/footer %>