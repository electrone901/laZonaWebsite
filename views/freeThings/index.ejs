<% include ../partials/header %>

    <header class="jumbotron">
        <div class="container">
            <h1>Free Thing</h1>
            <p>
                <a class="btn btn-primary btn-lg" href="/freeThings/new">Add New Free Thing</a>
            </p>
            <form action="/freeThings" method="GET" class="form-inline">
                <div class="form-group">
                    <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                    <input type="text" name="search" placeholder="Free Thing Search..." class="form-control">
                    <input type="submit" value="Search" class="btn btn-default">
                </div>
            </form>
        </div>
    </header>
    
    <div class="row text-center flex" >
        <div class="col-md-12">
            <% if(noMatch !== null){ %>
                <h3><%= noMatch %></h3>
            <% } %>
        </div>
        <% freeThings.forEach(function(freeThing){ %>
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                    <a href="/freeThings/<%= freeThing._id %>">
                        <img src="<%= freeThing.image %>">
                    </a>
                    <div class="caption">
                        <h4><%= freeThing.title %></h4>
                    </div>
                    <p>
                        <a href="/freeThings/<%= freeThing._id %>" class="btn btn-primary">More Info</a>
                    </p>
                </div>
            </div>
        <% }); %>
    </div>

<% include ../partials/footer %>