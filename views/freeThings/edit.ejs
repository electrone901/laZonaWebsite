<% include ../partials/header %>

    <div class="row">
        <h1 class="center">Edit <%= freeThing.title %></h1>
        <div class="form">
            <form action="/freeThings/<%= freeThing._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label class="label label-info" for="title">Title</label>
                    <input class="form-control" type="text" name="freeThing[title]" value="<%= freeThing.title %>">
                </div>
                <div class="form-group">
                    <label for="image">Image</label>
                    <input type="file" id="image" name="image" accept="image/*">
                </div>
                <div class="form-group">
                    <label class="label label-info" for="description">Description</label>
                    <textarea class="form-control" name="freeThing[description]" rows="5"><%= freeThing.description %></textarea>
                </div>
                <div class="form-group">
                    <label class="label label-info" for="location">Location</label>
                    <input class="form-control" type="text" name="freeThing[location]" value="<%= freeThing.location %>">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit</button>
                </div>
            </form>
            <a href="/freeThings/<%= freeThing._id %>">Go back</a>
        </div>
    </div>

<% include ../partials/footer %>