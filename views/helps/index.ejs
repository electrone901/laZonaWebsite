<% include ../partials/header %>

    <header class="jumbotron" id="orange">
        <div class="container">
            <h1>Ayuda</h1>
            <p>
                <a class="btn btn-primary btn-lg" href="/helps/new">Agregar Nuevas Ayudas</a>
            </p>
            <form action="/helps/title" method="GET" class="form-inline">
                <div class="form-group">
                    <input type="text" name="search" placeholder="Búsqueda por Título ..." class="form-control">
                    <button class="btn btn-default">
                        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                        Buscar
                    </button>
                </div>
            </form>
        </div>
    </header>
    
    <div class="row">
        <div class="col-md-12">
            <% if(noMatch !== null){ %>
                <h3 class="text-center"><%= noMatch %></h3><br>
            <% } %>
        </div>
        <% helps.forEach(function(help){ %>
            <div class="col-md-12 col-sm-6">
                <div class="thumbnail ridgeBorder">
                    <a href="/helps/<%= help._id %>"><p><%= help.title %></p></a>
                    <p><%- help.description.substring(0, 100) %>...</p>
                    <span><%= moment(help.createdAt).fromNow() %> by <a href="/profile/<%= help.author.id %>"><%= help.author.username %></span></a>
                </div>
            </div>
        <% }); %>
    </div>

<% include ../partials/footer %>