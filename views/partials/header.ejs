<!DOCTYPE html>
<html>
    <head>
        <title>Zone</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="/">The Zone</a>
                    
                </div>
                <ul class="nav navbar-nav">
                    <li><a class="navbar-brand" href="/jobs">Jobs</a></li>
                    <l1><a class="navbar-brand" href="/education">Education</a></l1>
                    <l1><a class="navbar-brand" href="/apartments">Apartments</a></l1>
                </ul>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <% if (!currentUser){ %>
                            <li><a href="/login">Login</a></li>
                            <li><a href="/register\">Sign Up</a></li>
                        <% }else { %>
                            <li><a href="/profile/<%= currentUser.id%>/edit">Signed in as <%= currentUser.username %></a></li>
                            <li><a href="/logout">Logout</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
        <div class="container">
            <% if(error && error.length > 0){ %>
                 <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length > 0){ %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>
       