<% include ../partials/header %>

    <header class="jumbotron">
        <div class="container">
            <h1>Apartments</h1>
            <p>
                <a class="btn btn-primary btn-lg" href="/apartments/new">Add New Apartment</a>
            </p>
            <form action="/apartments" method="GET" class="form-inline">
                <div class="form-group">
                    <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                    <input type="text" name="search" placeholder="Apartments Search..." class="form-control">
                    <input type="submit" value="Search" class="btn btn-default">
                </div>
            </form>
        </div>
    </header>
    
    <div class="row text-center flex" >
        <div class="col-md-12">
            <% if(noMatch !== null){ %>
                <h3><%= noMatch %></h3>
            <% } %>
        </div>
        <% apartments.forEach(function(apartment){ %>
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                    <img src="<%= apartment.image %>">
                    <div class="caption">
                        <h4><%= apartment.name %></h4>
                        <h5><%= apartment.city %>, <%= apartment.state %></h5>
                        <h5>$<%= apartment.price %></h5>
                    </div>
                    <p>
                        <a href="/apartments/<%= apartment._id %>" class="btn btn-primary">More Info</a>
                    </p>
                </div>
            </div>
        <% }); %>
    </div>

<% include ../partials/footer %>