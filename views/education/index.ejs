<% include ../partials/header %>

    <header class="jumbotron">
        <div class="container">
            <h1>Education</h1>
            <p>
                <a class="btn btn-primary btn-lg" href="/education/new">Add New Education</a>
            </p>
            <form action="/education" method="GET" class="form-inline">
                <div class="form-group">
                    <input type="text" name="search" placeholder="Subject Search..." class="form-control">
                    <input type="submit" value="Search" class="btn btn-default">
                </div>
            </form>
        </div>
    </header>
    
    <div class="row">
        <div class="col-md-12">
            <% if(noMatch !== null){ %>
                <h3><%= noMatch %></h3>
            <% } %>
        </div>
        <% education.forEach(function(subject){ %>
            <div class="col-md-12 col-sm-6">
                <div class="thumbnail">
                    <a href="/education/<%= subject._id %>"><p><%= subject.title %> - <%= subject.description %></p></a>
                    <span><%= subject.date.toDateString() %> by <%= subject.author.username %></span>
                </div>
            </div>
        <% }); %>
    </div>

<% include ../partials/footer %>